
const { getElections } = require("../controllers/election.controller");

const express = require("express")
const router = express.Router()

// router.get("/getElections", getElections)

module.exports = router;
